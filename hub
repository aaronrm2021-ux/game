<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Arcade Hub</title>
<style>
  /* Reset + fonts */
  body, html { margin:0; padding:0; font-family: 'Segoe UI', Arial, sans-serif; overflow-x:hidden; }
  body { background:#0f0f0f; color:white; }
  h1,h2,h3,p { margin:0; }
  /* Particle background */
  #particles { position:fixed; top:0; left:0; width:100%; height:100%; z-index:-1; }
  /* Lock screen */
  #lockscreen { height:100vh; display:flex; flex-direction:column; justify-content:center; align-items:center; background:linear-gradient(45deg,#1a1a1a,#0f0f0f); }
  #lockscreen h1 { font-size:3em; margin-bottom:20px; color:#0ff; text-shadow:0 0 10px #0ff; }
  #lockscreen input { padding:12px 20px; border-radius:8px; border:none; outline:none; font-size:1.2em; width:200px; margin-bottom:12px; }
  #lockscreen button { padding:10px 18px; border-radius:8px; border:none; background:#0ff; color:#000; font-weight:bold; cursor:pointer; font-size:1em; transition:0.2s; }
  #lockscreen button:hover { background:#0aa; color:white; }
  #msg { color:#f00; margin-top:8px; }
  /* Hub */
  #hub { display:none; padding:30px; max-width:1200px; margin:0 auto; }
  #hub h2 { text-align:center; font-size:2.5em; margin-bottom:20px; text-shadow:0 0 15px #0ff; }
  #search { width:80%; max-width:400px; padding:10px; border-radius:8px; border:none; margin:0 auto 20px; display:block; font-size:1em; }
  .grid { display:grid; grid-template-columns:repeat(auto-fit,minmax(200px,1fr)); gap:20px; }
  .card { background:#111; padding:15px; border-radius:12px; box-shadow:0 0 10px #0ff55a,0 0 20px #0ff55a; transition: transform 0.3s, box-shadow 0.3s; text-align:center; }
  .card:hover { transform:translateY(-10px) scale(1.05); box-shadow:0 0 20px #0ff,0 0 30px #0ff; }
  .card button { padding:8px 15px; border:none; border-radius:6px; background:#0ff; color:#000; cursor:pointer; margin-top:10px; font-weight:bold; transition:0.2s; }
  .card button:hover { background:#0aa; color:white; }
  #adminBtn { display:none; margin:20px auto; background:#f00; color:white; font-weight:bol
